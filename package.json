{
  "name": "sexsocialization-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@vercel/blob": "^2.6.1"
  },
  "engines": {
    "node": "22.x"
  }
}
